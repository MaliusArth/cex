

https://blog.molecular-matters.com/2011/06/29/designing-extensible-modular-classes/

ich hab den policy based logger aus deinem blog implementiert
und hab beim dispatch das problem dass die ellipse
in der Log funktion keine werte erlaubt aber die ellipse im makro nicht wirklich

#define MA_INFO(channel, format, ...) \
::ma::log::Logger::Dispatch(channel, ::ma::log::Type::Info, ::ma::log::g_verbosity, MA_SOURCE_INFO, format, __VA_ARGS__);

aufrufe:
//funktioniert
MA_INFO("Main", "Inject++ Tool is running.%s", ".. yeah!");

// clang und gcc schmeißen da "warning: must specify atleast one argument for '...' parameter of variadic macro [-Wgnu-zero-variadic-macro-arguments]"
MA_INFO("Main", "Inject++ Tool is running.");

im internet hab ich kaum hilfreiche workarounds gefunden,
da ich format separat behandeln können muss kann ich nicht einfach (channel, ...) machen...
und selbst wenn, vom API design ist das ja nicht so toll

Bei dir scheint das ja laut deinem C++ as a scripting language blog post zu funktionieren
https://blog.molecular-matters.com/2014/05/10/using-runtime-compiled-c-code-as-a-scripting-language-under-the-hood/

ME_ERROR("ScriptSupport", "An exception has been caught, forcing the script to be disabled. See the log output for more details.");

hast du da vlt. irgendeine macro magic parat?
hab gelesen dass in MSVC variadic macros anders expanded werden und das problem nicht auftritt

