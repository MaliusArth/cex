#pragma once

#include "exports.hpp"

// Returns n! (the factorial of n). For negative n, n! is defined to be 1.
 MA_API int Factorial(int n);

// Returns true iff n is a prime number.
 MA_API bool IsPrime(int n);
